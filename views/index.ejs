<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon site avec EJS</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="/styles/info_resultat.css">
</head>
<body>
    <img src="/images/wildlens.png" class="bandeau">
    
    <div class="video_align">
        <video id="videoElement" autoplay> Votre navigateur ne détecte aucune caméra.</video>
        <canvas id="canvasElement" style="display:none;"></canvas>

        <script>
            // Obtenez l'élément vidéo
            const video = document.getElementById('videoElement');

            // Demandez l'accès à la caméra
            navigator.mediaDevices.getUserMedia({ video: true })
                .then(stream => {
                    video.srcObject = stream;
                })
                .catch(err => {
                    console.error("Erreur: " + err);
                });
        </script>
    </div>

    <div>
        <img src="/images/logo_bouton_vert.png" class="bouton_principal" id="imageCliquable">
    </div>
        
    <script>
        // Sélectionnez le bouton par son ID
        const bouton = document.getElementById('imageCliquable');
        const canvas = document.getElementById('canvasElement');
        const context = canvas.getContext('2d');

        // Ajoutez un écouteur d'événement pour le clic
        bouton.addEventListener('click', function() {
            // Affichez le canvas et copiez le contenu vidéo dans le canvas
            canvas.style.display = 'block';
            context.drawImage(video, 0, 0, canvas.width, canvas.height);
        
        // Convertissez le contenu du canvas en une image
        const image = canvas.toDataURL('image/png');
        
        // Créez un nouvel élément d'image pour afficher l'image capturée (vous pouvez également l'envoyer au serveur ou la télécharger)
        const capturedImage = document.createElement('img');
        capturedImage.src = image;
        document.body.appendChild(capturedImage);  // Ajoutez l'image capturée au corps de la page (ou où vous le souhaitez)
    });

        // Assurez-vous que le canvas ait la même taille que la vidéo
        video.addEventListener('loadedmetadata', function() {
            canvas.width = video.videoWidth;
            canvas.height = video.videoHeight;
});
    </script>
    <div class="info_container show"> 
        <!-- optimiser le code: répétition de row -->
        <div class="column">
            <div class="row">
                <img src="/images/img_loup.jpg" class="info_animal_img">
            </div>
            <div class="row">
                <h1>Loup</h1>
            </div>
            <div class="row">
                <p class="info_animal_txt">Quanta autem vis amicitiae sit, ex hoc intellegi maxime potest, quod ex infinita societate generis humani, quam conciliavit ipsa natura.</p>
            </div>
            <div class="row">
                <div class="column">
                    <h2 class="petit-txt">Nom Latin</h2>
                    <div class="container_info"></div>
                    <h2 class="petit-txt">Famille</h2>
                    <div class="container_info"></div>
                </div>
                <div class="separateur"></div>
                <div class="column">
                    <h2 class="petit-txt">Région</h2>
                    <div class="container_info">ezfiezbfjzebfyezvyigeftubezihfbvzetyvgubhnjbhvgcr-deryvubhinhbvcrdervgbhinhbuvycrdev</div>
                    <h2 class="petit-txt">Taille</h2>
                    <div class="container_info"></div>
                </div>
            </div>
            <div class="row">
                <div class="column">
                    <h2>Habitat</h2>
                    <p class="info_animal_txt">Quanta autem vis amicitiae sit, ex hoc intellegi maxime potest, quod ex infinita societate generis humani.</p>
                </div>  
            </div>
            <div class="row">
                <div class="column">
                    <h2>Fun Fact</h2>
                    <p class="info_animal_txt">Quanta autem vis amicitiae sit, ex hoc intellegi maxime potest, quod ex infinita societate generis humani.</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
